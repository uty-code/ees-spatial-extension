# 🗺️ Intelligent Spatial BI Evaluation System Guide

본 문서는 EES(Evaluation Expansion System)의 핵심 로직인 공간 지능 기반의 인사/운영 평가 시스템에 대한 가이드를 제공합니다.

---

## 1. 핵심 분석 개념 (Core Concepts)

### 📊 평가 격차 분석 (Evaluation Gap Analysis)
본 시스템의 가장 큰 특징은 **데이터(정량)**와 **사람의 판단(정성)** 사이의 차이를 분석하는 것입니다.

*   **정량 평가 (Operational Score)**: KPI, 매출, 위생 점검 등 객관적인 수치로 도출된 점수 (60% 반영)
*   **정성 평가 (Manager Score)**: 구역 관리자 또는 부서장이 현장 방문 및 인터뷰를 통해 매긴 점수 (40% 반영)
*   **Gap (격차)**: `|정성 평가 점수 - 정량 평가 점수|`
    *   격차가 크다는 것은 데이터상으로는 우수하나 실제 관리가 부실하거나, 반대로 실적은 낮으나 관리 노력은 뛰어난 경우를 의미합니다.

### 📍 공간 지능 (Spatial Intelligence)
단순한 지표 비교를 넘어, 지점이 위치한 **상권 유형(Region Type)**과 **지리적 환경(Context)**을 분석에 포함합니다.

*   **상권 기반 분석 반경 (Region-based Radius)**: 상권의 혼잡도에 따라 밀도 분석 반경을 다르게 적용합니다.
    *   **도심형 (URBAN_CORE)**: 반경 **300m** 분석
    *   **일반형 (GENERAL_CITY)**: 반경 **500m** 분석
    *   **외곽형 (SUBURBAN)**: 반경 **1000m** 분석
*   **상권별 차등 밀도 기준 (Context-aware Thresholds)**: 동일한 지점 수라도 상권 유형에 따라 밀도 등급(LOW/MID/HIGH)을 다르게 판정합니다.
*   **보정 정책 (Adjustment Policy)**: 운영 난이도 보정을 위해 성과 점수에 계수(Multiplier)를 적용하되, 평가 왜곡 방지를 위해 **Bonus Cap(상한선)**을 둡니다.
    *   **HIGH**: 1.05배 적용 (최대 **+5점** 상한)
    *   **MID**: 1.03배 적용 (최대 **+3점** 상한)
    *   **LOW**: 보정 없음 (1.00배)
*   **공간 스냅샷 (Spatial Snapshot)**: 평가 확정 시점의 공간 분석 상태를 JSON 형태로 영구 보존하여 사후 감사(Audit) 시 활용합니다.

---

## 2. 위험도 판단 기준 (Risk Assessment)

시스템은 아래의 기준에 따라 각 지점의 위험 수준을 자동으로 분류합니다.

| 위험 등급 | 상태 (Status) | 판단 기준 (Gap) | 의미 및 조치 권고 |
| :--- | :--- | :--- | :--- |
| **🔴 HIGH** | **ANOMALY** | **30점 이상** | 데이터 왜곡 가능성 매우 높음. 즉시 현장 실사 및 평가 프로세스 재검토 필요. |
| **🟡 MEDIUM** | **WARNING** | **15점 ~ 30점** | 평가 불일치 주의 수준. 상급자의 정밀 리뷰 권고. |
| **🟢 LOW** | **NORMAL** | **15점 미만** | 데이터와 현장 평가가 일치함. 신뢰할 수 있는 운영 상태. |

---

## 3. 지도 시각화 가이드 (Map Visualization)

지점 데이터는 카카오맵 API를 통해 시각화되며, 사용자의 줌(Zoom) 레벨에 따라 유동적으로 변화합니다.

### 📌 마커 (Marker) vs 클러스터 (Cluster)
*   **마커 (Marker)**: 개별 지점의 정확한 위치를 표시합니다. 마커의 **색상**은 해당 지점의 **공간 밀도(Density)**를 나타냅니다.
*   **클러스터 (Cluster)**: 여러 마커를 하나의 원으로 요약합니다. 클러스터의 **색상**은 내부 지점들의 **리스크 비율(Risk Ratio)**을 나타냅니다.
    *   **숫자의 의미**: 해당 구역에 포함된 **지점(매장)의 총 개수**입니다.

### 🎨 색상 코드 (Color Coding)
*   **마커 색상 (공간 밀도)**: 상권 유형별 차등 임계값에 따라 결정됩니다.
    *   **URBAN_CORE**: 9개 이상(HIGH), 5-8개(MID), 4개 이하(LOW)
    *   **GENERAL_CITY**: 6개 이상(HIGH), 3-5개(MID), 2개 이하(LOW)
    *   **SUBURBAN**: 4개 이상(HIGH), 2-3개(MID), 1개 이하(LOW)
*   **클러스터 색상 (위험 집계)**: 클러스터 내 ANOMALY/WARNING 지점의 비중을 계산합니다.
    *   **빨강 (Critical)**: 리스크 비율 70% 이상
    *   **노랑 (Warning)**: 리스크 비율 30% 이상
    *   **파랑 (Stable)**: 리스크 비율 30% 미만 또는 지점 수 3개 미만

---

## 4. 데이터베이스 및 업데이트 가이드
*   **DB 스키마 확장**:
    *   `branches_51.region_type`: 상권 분류 (URBAN_CORE, GENERAL_CITY, SUBURBAN)
    *   `final_grades_51.spatial_snapshot`: 평가 시점의 분석 데이터 (JSON)
*   **정성 평가**: 관리자 페이지에서 인터뷰 및 현장 점검 결과가 입력되어야 최종 점수와 Gap이 계산됩니다.
*   **정량 데이터**: 매 분기별 Batch 작업을 통해 운영 KPI 데이터가 업데이트됩니다.
*   **공간 데이터**: 신규 지점 등록 시 주소를 기반으로 위/경도 좌표가 자동 생성되며 밀집도 분석에 포함됩니다.
