<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ees.eval.mapper.BatchJobHistoryMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="jobId">
        insert into batch_job_history_51 (
            job_name, period_id, status, start_time, result_message, created_by
        ) values (
            #{jobName}, #{periodId}, #{status}, #{startTime}, #{resultMessage}, #{createdBy}
        )
    </insert>

    <update id="update">
        update batch_job_history_51
        set status = #{status},
            end_time = #{endTime},
            result_message = #{resultMessage},
            version = version + 1
        where job_id = #{jobId}
    </update>

    <select id="findById" resultType="com.ees.eval.domain.BatchJobHistory">
        select
            job_id, job_name, period_id, status, start_time, end_time, result_message, version, created_at, created_by
        from batch_job_history_51
        where job_id = #{jobId}
    </select>

    <select id="findByJobName" resultType="com.ees.eval.domain.BatchJobHistory">
        select
            job_id, job_name, period_id, status, start_time, end_time, result_message, version, created_at, created_by
        from batch_job_history_51
        where job_name = #{jobName}
        order by created_at desc
    </select>

</mapper>
