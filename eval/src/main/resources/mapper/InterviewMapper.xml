<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ees.eval.mapper.InterviewMapper">

    <resultMap id="InterviewResultMap" type="com.ees.eval.domain.Interview">
        <id property="interviewId"     column="interview_id"/>
        <result property="mappingId"    column="mapping_id"/>
        <result property="content1"     column="content1"/>
        <result property="content2"     column="content2"/>
        <result property="content3"     column="content3"/>
        <result property="content4"     column="content4"/>
        <result property="statusCode"   column="status_code"/>
        <result property="isDeleted"    column="is_deleted"/>
        <result property="version"      column="version"/>
        <result property="createdAt"    column="created_at"/>
        <result property="createdBy"    column="created_by"/>
        <result property="updatedAt"    column="updated_at"/>
        <result property="updatedBy"    column="updated_by"/>
    </resultMap>

    <sql id="InterviewColumns">
        interview_id, mapping_id, content1, content2, content3, content4,
        status_code, is_deleted, version, created_at, created_by, updated_at, updated_by
    </sql>

    <!-- 단건 조회 (일반) -->
    <select id="findByMappingId" resultMap="InterviewResultMap">
        SELECT <include refid="InterviewColumns"/>
        FROM interviews_51
        WHERE mapping_id = #{mappingId}
          AND is_deleted = 'n'
    </select>

    <!-- Batch 조회 (SARG 준수, IN 절 사용) -->
    <select id="findByMappingIds" resultMap="InterviewResultMap">
        SELECT <include refid="InterviewColumns"/>
        FROM interviews_51
        WHERE mapping_id IN
        <foreach item="id" collection="mappingIds" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND is_deleted = 'n'
    </select>

    <!-- 신규 면담 기록 삽입 -->
    <insert id="insert" parameterType="com.ees.eval.domain.Interview">
        INSERT INTO interviews_51 (
            mapping_id, content1, content2, content3, content4, status_code,
            is_deleted, version, created_at, created_by, updated_at, updated_by
        ) VALUES (
            #{mappingId}, #{content1}, #{content2}, #{content3}, #{content4}, #{statusCode},
            #{isDeleted}, #{version}, #{createdAt}, #{createdBy}, #{updatedAt}, #{updatedBy}
        )
    </insert>

    <update id="update" parameterType="com.ees.eval.domain.Interview">
        UPDATE interviews_51
        <set>
            <if test="content1 != null">content1 = #{content1},</if>
            <if test="content2 != null">content2 = #{content2},</if>
            <if test="content3 != null">content3 = #{content3},</if>
            <if test="content4 != null">content4 = #{content4},</if>
            <if test="statusCode != null">status_code = #{statusCode},</if>
            updated_at = #{updatedAt},
            updated_by = #{updatedBy},
            version = version + 1
        </set>
        WHERE interview_id = #{interviewId}
          AND version = #{version}
    </update>

</mapper>
