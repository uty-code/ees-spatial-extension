<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ees.eval.mapper.EvaluationSnapshotMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="snapshotId">
        insert into evaluation_snapshots_51 (
            period_id, mapping_id, snapshot_data, snapshot_version, source_type
        ) values (
            #{periodId}, #{mappingId}, #{snapshotData}, #{snapshotVersion}, #{sourceType}
        )
    </insert>

    <select id="findByMappingId" resultType="com.ees.eval.domain.EvaluationSnapshot">
        select
            snapshot_id, period_id, mapping_id, snapshot_data, snapshot_version, source_type, created_at
        from evaluation_snapshots_51
        where mapping_id = #{mappingId}
        order by created_at desc
    </select>

    <select id="findByPeriodId" resultType="com.ees.eval.domain.EvaluationSnapshot">
        select
            snapshot_id, period_id, mapping_id, snapshot_data, snapshot_version, source_type, created_at
        from evaluation_snapshots_51
        where period_id = #{periodId}
        order by created_at desc
    </select>

</mapper>
